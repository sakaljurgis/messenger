# Base URL the app is served from (used in webhook payloads and push config)
APP_ORIGIN=http://localhost:3001

# Web push — generate once with: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
